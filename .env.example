# ============================================================================
# Synapse — Environment Variables Template
# ============================================================================
# Copy this file to `.env` and fill in your values.
#   cp .env.example .env
#
# NEVER commit the real `.env` file — it contains secrets.
# ============================================================================

# --- Discord ----------------------------------------------------------------
# Create a bot at https://discord.com/developers/applications
# Bot → Token → Copy
DISCORD_TOKEN=your-discord-bot-token-here

# (Optional) Set this to a Guild/Server ID for instant slash-command syncing
# during development.  Leave blank for global sync (takes up to 1 hour).
DEV_GUILD_ID=

# --- Discord OAuth2 (Dashboard Admin Panel) --------------------------------
# OAuth2 → General → Client ID / Client Secret
# OAuth2 → Redirects → Add: http://localhost:3000/auth/callback
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
DISCORD_REDIRECT_URI=http://localhost:3000/auth/callback

# --- PostgreSQL -------------------------------------------------------------
# Format: postgresql+psycopg2://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgresql+psycopg2://synapse:synapse@localhost:5432/synapse

# --- JWT / Auth -------------------------------------------------------------
# Secret key for signing admin JWTs.  REQUIRED — the API refuses to start
# without a strong secret (>= 32 characters, not a known default).
# Generate one with:  python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET=

# --- Frontend ---------------------------------------------------------------
# Base URL of the SvelteKit dashboard (for auth redirects)
FRONTEND_URL=http://localhost:3000
